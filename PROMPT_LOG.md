# Лог взаимодействия с AI (Prompt Log)

**Студент:** Азаров Алексей Семенович
**Группа:** 221331-01
**Вариант:** 2
**Тема:** Система управления библиотекой

### Промпт 1: Генерация CRUD-приложения
> Ты — senior Python разработчик. Сгенерируй REST API на FastAPI для управления книгами в библиотеке. Модель Book должна содержать поля: id, title, author, isbn, published_year, genre, quantity, available_quantity. Реализуй эндпоинты GET /books, POST /books, GET /books/{id}, PUT /books/{id}, DELETE /books/{id}. Используй Pydantic для валидации (ISBN: 10 или 13 цифр, год публикации 1450-2026, available_quantity <= quantity). Добавь обработку ошибок и type hints.

### Промпт 2: Генерация тестов
> Напиши модульные тесты (pytest + httpx) для CRUD-эндпоинтов библиотеки. Используй SQLite in-memory для тестовой БД. Тесты должны покрывать: создание книги, создание дубликата ISBN, получение списка, получение по ID, обновление, удаление, поиск по названию, поиск несуществующей книги, невалидный ISBN, превышение available_quantity над quantity. Покрытие — не менее 70% ключевых маршрутов.

### Промпт 3: Рефакторинг плохого кода
> Вот плохо написанная функция для расчёта штрафов в библиотеке. Проблемы: магические числа, неинформативные имена (b, r, bk, d, p), дублирование кода, отсутствие обработки ошибок, длина > 30 строк, нет type hints, файловый ввод-вывод без контекстного менеджера. Выполни рефакторинг с объяснением каждого изменения.

### Промпт 4: Генерация Docker-конфигурации
> Создай Dockerfile и docker-compose.yml для FastAPI приложения управления библиотекой. Dockerfile на основе python:3.12-slim. docker-compose должен поднимать приложение на порту 8000 с поддержкой --reload для разработки. Используй .env для переменных окружения.

### Промпт 5: Объяснение сложного кода
> Объясни работу функции calculate_fine простым языком. Функция рассчитывает штраф за просрочку: базовая сумма x дни просрочки, скидка 10% для premium, кап 5 руб и вычет 2 руб для student. Предложи улучшения (Enum для категорий, Decimal вместо float, вынос тарифов в константы).

### Промпт 6: Генерация документации
> Создай README.md для проекта Library Management System. Включи: название, описание, инструкцию по установке (локально и через Docker), описание API-эндпоинтов с примерами JSON-запросов и ответов, таблицу переменных окружения, таблицу заданий с ссылками на файлы.

### Промпт 7: Генерация миграций БД
> Сгенерируй Alembic-миграцию для создания таблиц библиотеки: books (id, title, author, isbn unique, published_year, genre, quantity, available_quantity), readers (id, name, email unique, phone, registered_date), checkouts (id, book_id FK, reader_id FK, checkout_date, return_date, status), fines (id, checkout_id FK, amount, paid). Добавь индексы на title, author, isbn, email.

### Промпт 8: Поиск уязвимостей в коде
> Ты — security code reviewer. Найди уязвимости в FastAPI приложении библиотеки: SQL-инъекции, XSS, path traversal, отсутствие аутентификации, отсутствие rate limiting, незакрытые соединения, утечка информации через DEBUG. Дай отчёт с рекомендациями по исправлению.

### Промпт 9: Генерация SQL-запросов
> Напиши 5 аналитических SQL-запросов для библиотеки: (1) топ-10 самых читаемых книг, (2) читатели с наибольшими штрафами, (3) книги на руках (не возвращены), (4) статистика по жанрам (среднее время аренды), (5) эффективность сбора штрафов (% оплаченных). Используй SQLite-синтаксис.

### Промпт 10: Генерация регулярного выражения
> Сгенерируй регулярное выражение для валидации ISBN книги: 10 или 13 цифр, допускаются дефисы. Напиши Python-скрипт с набором валидных и невалидных примеров для тестирования.
